{
  server {
    address = "localhost"
    port = 7575
  }
  ledger-api {
    address = "localhost"
    port = 6865
  }
  # Allow WebSocket connections from Vite dev server
  access-control-allow-origin = "http://localhost:5173"
  
  # REQUIRED: Allow JWTs to be sent over plain HTTP/WS (localhost)
  allow-insecure-tokens = true
}